Django==5.0.6
cryptography==42.0.8
requests==2.32.3
Pillow==10.4.0
django-crispy-forms==2.3
crispy-bootstrap5==2024.2
gunicorn==22.0.0
python-dotenv==1.0.1
openpyxl==3.1.5
# Postgres driver — used only when DATABASE_URL points at Postgres (EC2/prod).
# Harmless to install locally; the SQLite dev default never imports it.
psycopg2-binary==2.9.9
