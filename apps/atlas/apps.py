from django.apps import AppConfig


class AtlasConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.atlas'
    verbose_name = 'Project Atlas — B2B Quote-to-Cash'
