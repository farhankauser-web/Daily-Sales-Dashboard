# Marketing — decision log

Decisions that a future session could reasonably choose differently. Recorded so
the same ground is not re-argued.

Never edit a decision to change its meaning. Add a new one, mark the old
`superseded by`, and say what changed.

| ID | Decision | Date | Status |
|---|---|---|---|

*No decisions recorded yet.* Expect several to be backfilled from the code and
commit history as the leaves are written — attribution rules in particular are
choices, not facts, and `apps/dashboard/ppc_allocator.py` encodes them.

Nine of Inventory's twenty decisions were backfilled this way; do it while the
code is open rather than at the end.
